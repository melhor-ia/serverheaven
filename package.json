{
  "name": "server-heaven",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "firebase serve",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "firebase": "^11.9.1",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "vite": "^5.0.0"
  }
}
